<?xml version='1.0' encoding='utf-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd">

<issue num="4582" status="New">
<title>Specializations of `layout_transpose` do not meet the layout mapping policy requirements</title>
<section>
<sref ref="[linalg.transp.layout.transpose]"/>
<sref ref="[mdspan.layout.policy.reqmts]"/>
</section>
<submitter>Charles Hussong</submitter>
<date>19 May 2026</date>
<priority>3</priority>

<discussion>
<p>
<sref ref="[linalg.transp.layout.transpose]"/> bullet (3.2) specifies a <i>Mandate</i> 
that the rank of the extents used with this layout <em>must</em> equal 2. However, the 
layout mapping policy requirements at <sref ref="[mdspan.layout.policy.reqmts]"/> are 
worded in the following way:
</p>
<blockquote>
<p>
A type `MP` meets the <i>layout mapping policy</i> requirements if for a type `E` that is a 
specialization of `extents`, <tt>MP::mapping&lt;E&gt;</tt> is valid and denotes a type 
`X` that meets the layout mapping requirements (<sref ref="[mdspan.layout.reqmts]"/>), 
[&hellip;]
</p>
</blockquote>
<p>
We interpret this to mean that in order for `MP` to be a layout mapping policy, it must 
be possible to instantiate <tt>MP::mapping&lt;E&gt;</tt> for <em>any</em> extents `E`, 
with no additional mandates. Since `layout_transpose` does not satisfy this, specializations
of it do not meet the layout mapping policy, which contradicts <sref ref="[linalg.transp.intro]"/> p1, 
and <sref ref="[linalg.transp.layout.transpose]"/> p1, and makes any `mdspan` using 
`layout_transpose` (including the return type of `transposed`(<sref ref="[linalg.transp.transposed]"/>)) 
ill-formed.
<p/>
I think the most straightforward solution to this would be to change the layout mapping policy 
requirements:
</p>
<blockquote>
<p>
A type `MP` meets the <i>layout mapping policy</i> requirements if for <del>a</del><ins>at least one</ins> 
type `E` that is a specialization of `extents`, <tt>MP::mapping&lt;E&gt;</tt> is valid and denotes 
a type `X` that meets the layout mapping requirements [&hellip;]
</p>
</blockquote>
<p>
Unfortunately this is a much looser requirement, but I'm not sure how else to accommodate a sensible 
definition of `layout_transpose` within the stricter version.
<p/>
<b>Considered alternatives:</b>
<p/>
`layout_transpose` could be redefined to either always transpose the first two or last two ranks. 
I did not submit this because it could easily get confusing for users (was it the first two or 
the last two, or maybe something else entirely?) and there's no sensible behavior for rank 1.
<p/>
Another option would be to replace the notion of "meet the layout mapping policy requirements" 
with "meet the layout mapping policy requirements for extents `E`". My understanding is that this 
goes against the spirit of the design where the layout mapping policy is not templated on the 
specific extents, while the layout mapping is.
</p>

<superseded>
<p>
This wording is relative to <paper num="N5046"/>.
</p>

<ol>
<li>
<p>Modify <sref ref="[mdspan.layout.policy.reqmts]"/> as indicated:</p>

<blockquote>
<p>
-1- A type `MP` meets the <i>layout mapping policy</i> requirements if for <del>a</del><ins>at least one</ins> 
type `E` that is a specialization of `extents`, <tt>MP::mapping&lt;E&gt;</tt> is valid and denotes 
a type `X` that meets the layout mapping requirements (<sref ref="[mdspan.layout.reqmts]"/>), and for
which the <i>qualified-id</i> `X::layout_type` is valid and denotes the type `MP` and the 
<i>qualified-id</i> `X::extents_type` denotes `E`.
</p>
</blockquote>
</li>
 </ol>
</superseded>

<note>2026-06-16, Mark Hoemmen comments and provides improved wording</note>
<p>
I don't like this. A layout is supposed to be a "function" from
extents type to layout mapping type. We messed up with accessors; we
should have done the same thing to make an "accessor policy" that is a
function from element type to accessor type. (That would have solved
the "given an mdspan with nonconst element type, how do I get an
mdspan viewing the same elements with const element type?" problem.)
<p/>
It might make sense to let layouts restrict the set of valid ranks.
That would fix the issue without requiring a redesign of `transposed`.
I suggest the following fix.
</p>

<note>2026-08-27; Reflector poll.</note>
<p>
Set priority to 3 after reflector poll.
</p>
</discussion>

<resolution>
<p>
This wording is relative to <paper num="N5046"/>.
</p>

<ol>
<li>
<p>Modify <sref ref="[mdspan.layout.policy.reqmts]"/> as indicated:</p>

<blockquote>
<p>
-1- A type `MP` meets the <i>layout mapping policy</i> requirements if 
<ins>there exists at least one rank index `r` such that,</ins>
for a type `E` <ins>of rank `r`</ins> that is a specialization of `extents`, 
<tt>MP::mapping&lt;E&gt;</tt> is valid and denotes a type `X` that meets the 
layout mapping requirements (<sref ref="[mdspan.layout.reqmts]"/>), and for
which the <i>qualified-id</i> `X::layout_type` is valid and denotes the type 
`MP` and the <i>qualified-id</i> `X::extents_type` denotes `E`.
</p>
</blockquote>

</li>

 </ol>


</resolution>

</issue>
