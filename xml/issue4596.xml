<?xml version='1.0' encoding='utf-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd">

<issue num="4596" status="New">
<title>Gratuitous UB via manifestly constant-evaluated contexts in signal handlers</title>
<section><sref ref="[support.signal]"/></section>
<submitter>Hubert Tong</submitter>
<date>10 June 2026</date>
<priority>3</priority>

<discussion>
<p>
<sref ref="[support.signal]"/> paragraph 2 specifies that a signal handler invocation has undefined behaviour 
if it includes an evaluation that is not signal-safe.
<p/>
Such an included evaluation (consistent with CWG's understanding; see 
<a href="https://cplusplus.github.io/CWG/issues/3162.html">CWG 3162</a>) may occur within a manifestly 
constant-evaluated context, in which case, the specified undefined behaviour is gratuitous.
<p/>
[Example from Jens Maurer:] The issue here is:
</p>
<blockquote><pre>
constexpr int f()
{
  try { throw 0; } catch (...) {}  // not signal-safe <sref ref="[support.signal]"/> bullets 3.4 and 3.5
  return 0;
}

void handler(int)  // signal handler
{
  constexpr int x = f();  // ok?
}
</pre></blockquote>
<p>
The evaluation of "handler" includes the evaluation of `f`, which is not signal-safe.
But that's not really a good state of affairs, because the constant evaluation of
`f` certainly has no runtime impact on the signal handler.
</p>

<note>2026-08-27; Reflector poll.</note>
<p>
Set priority to 3 after reflector poll.
</p>
</discussion>

<resolution>
</resolution>

</issue>
