<?xml version='1.0' encoding='utf-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd">

<issue num="4615" status="New">
<title>`stride_view` iterator operations are missing preconditions</title>
<section><sref ref="[range.stride.iterator]"/></section>
<submitter>S. B. Tam</submitter>
<date>17 Aug 2026</date>
<priority>3</priority>

<discussion>
<p>
`stride_view`'s iterator comparison and subtraction do not require the iterators to come from the 
same range and have the same stride, and thus seem to permit nonsensical code like:
</p>
<blockquote><pre>
#include &lt;ranges&gt;

int arr[] = {1, 2, 3, 4, 5, 6, 7};
auto r1 = std::views::stride(arr, 2);
auto r2 = std::views::stride(arr, 6);

auto it1 = r1.end(); // it1.<i>missing_</i> == 1
auto it2 = r2.end(); // it2.<i>missing_</i> == 5

auto x = r1.end() - r2.end(); // x == (0 + 1 - 5) / 2
</pre></blockquote>
<p>
Should these operations have preconditions to treat such cases as UB?
</p>

<note>2026-08-27; Reflector poll.</note>
<p>
Set priority to 3 after reflector poll.
</p>
</discussion>

<resolution>
</resolution>

</issue>
